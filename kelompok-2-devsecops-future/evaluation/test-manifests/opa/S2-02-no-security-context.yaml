# Expected: DENY (tidak ada securityContext sama sekali -> runAsNonRoot tidak true)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-s2-02
  namespace: taskflow-prod
spec:
  replicas: 1
  selector:
    matchLabels: {app: test-s2-02}
  template:
    metadata:
      labels: {app: test-s2-02}
    spec:
      containers:
        - name: app
          image: registry.gitlab.com/haidarra-devops/week-9-devops-taskflow:sha-abc1234
          resources:
            requests: {cpu: "100m", memory: "128Mi"}
            limits: {cpu: "500m", memory: "256Mi"}
