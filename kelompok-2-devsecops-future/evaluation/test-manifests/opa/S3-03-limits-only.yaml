# Expected: DENY (ada limits, tidak ada requests)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-s3-03
  namespace: taskflow-prod
spec:
  replicas: 1
  selector:
    matchLabels: {app: test-s3-03}
  template:
    metadata:
      labels: {app: test-s3-03}
    spec:
      containers:
        - name: app
          image: registry.gitlab.com/haidarra-devops/week-9-devops-taskflow:sha-abc1234
          resources:
            limits: {cpu: "500m", memory: "256Mi"}
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
