# Expected: DENY (runAsUser: 0 = root)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-s2-01
  namespace: taskflow-prod
spec:
  replicas: 1
  selector:
    matchLabels: {app: test-s2-01}
  template:
    metadata:
      labels: {app: test-s2-01}
    spec:
      containers:
        - name: app
          image: registry.gitlab.com/haidarra-devops/week-9-devops-taskflow:sha-abc1234
          resources:
            requests: {cpu: "100m", memory: "128Mi"}
            limits: {cpu: "500m", memory: "256Mi"}
          securityContext:
            runAsUser: 0
