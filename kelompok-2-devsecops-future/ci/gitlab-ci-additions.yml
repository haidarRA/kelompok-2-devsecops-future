# ============================================================
# REFERENCE: GitLab CI Additions — Paper A Enhancement
# ============================================================
#
# This file documents the changes made to the baseline
# `.gitlab-ci.yml` for the Paper A (OPA/Conftest) enhancement.
#
# The COMPLETE merged pipeline is already in the root
# `.gitlab-ci.yml` at the enhancement repository root.
# This snippet is kept here as a reference to show what
# was added relative to the Week 12 baseline.
#
# Changes made:
#   1. Added `policy-check` to the `stages:` list
#      (positioned between `build` and `package`)
#   2. Added the `validate-k8s-manifest` job below
#
# Paper B (Falco + Kyverno) enhancements are runtime-only
# and do not require CI pipeline changes.
# ============================================================
#
# --- BEGIN SNIPPET ---
# validate-k8s-manifest:
#   stage: policy-check
#   image: openpolicyagent/conftest:v0.55.0
#   needs: ["compile-binary"]
#   before_script: []
#   script:
#     - echo "Menjalankan OPA policy check terhadap manifest Kubernetes..."
#     - conftest test kubernetes/deployment.yaml --policy policies/opa/ --output table
#   <<: *notify_template
#   only:
#     - main
#     - develop
# --- END SNIPPET ---
