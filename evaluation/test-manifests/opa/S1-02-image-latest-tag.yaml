# Expected: DENY (tag :latest tidak boleh dipakai)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-s1-02
  namespace: taskflow-prod
spec:
  replicas: 1
  selector:
    matchLabels: {app: test-s1-02}
  template:
    metadata:
      labels: {app: test-s1-02}
    spec:
      containers:
        - name: app
          image: registry.gitlab.com/haidarra-devops/week-9-devops-taskflow:latest
          resources:
            requests: {cpu: "100m", memory: "128Mi"}
            limits: {cpu: "500m", memory: "256Mi"}
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
