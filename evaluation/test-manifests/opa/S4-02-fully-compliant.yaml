# Expected: ALLOW (manifest lengkap, sesuai semua policy) - variasi 02
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-s4-02
  namespace: taskflow-prod
spec:
  replicas: 1
  selector:
    matchLabels: {app: test-s4-02}
  template:
    metadata:
      labels: {app: test-s4-02}
    spec:
      containers:
        - name: app
          image: registry.gitlab.com/haidarra-devops/week-9-devops-taskflow:sha-abc1202
          resources:
            requests: {cpu: "100m", memory: "128Mi"}
            limits: {cpu: "500m", memory: "256Mi"}
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            privileged: false
